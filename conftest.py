"""Корінь проєкту для pytest — гарантує, що пакет `Dekanat` імпортується у тестах.

Наявність conftest.py у корені додає цю теку до `sys.path`, тож `import Dekanat`
працює під час прогону `uv run pytest` з будь-якої підтеки.
"""
